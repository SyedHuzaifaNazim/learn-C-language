#include <stdio.h>
#include <stdlib.h>
#include <strings.h>
#include <windows.h>
#include <math.h>

struct Account{
	int choice;
	char name[2];
	int rollno;
};
void fall();

int main()
{
	char arr;
	printf("Account??>>  ");
	scanf("%[^\n]",&arr);
	if(arr>0)
	{
		fall();
	}
//	if(IDE==choice)
}

void fall()
{
	("what: ");
//	gets("%[^\n]",&Account,fall->choice);
}
void html()
{
	printf("hello");
}
